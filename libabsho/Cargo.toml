[package]
name = "libabsho"
version = "1.1.4"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
runtime = { path = "../runtime", features = ["core", "checkout", "checkout_ng"] }
tokio = { version = "1", features = ["full"] }
