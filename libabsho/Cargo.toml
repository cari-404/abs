[package]
name = "libabsho"
version = "1.1.2"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
runtime = { path = "../runtime", features = ["core", "checkout", "checkout_ng"] }
tokio = { version = "1", features = ["full"] }
